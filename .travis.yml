jobs:
  include:
    - os: linux
      dist: trusty
    
    - os: linux
      dist: xenial
    
    - os: linux
      dist: focal 

    - os: osx

language: python
python:
  - "3.9-dev"

before_script:
  - pip install tox-travis

script:
  - tox
